export type ThemeColor = {
    shadow: string
    border: string
    text: string
    button: string
    icon: string
}

export type Category = {
    id: string
    title: string
    description: string
    image: string
    layout: string
    specialEffect: string
}